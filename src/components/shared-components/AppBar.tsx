import React from 'react';
import HomeNavbar from '../HomeNavbar';

const AppBar = () => {
  return (
    <header className="flex gap-4  text-white fixed">
      <HomeNavbar />
    </header>
  );
};

export default AppBar;
